[pytest]
pythonpath = .
testpaths = tests
markers =
    unit: Isolated logic tests — no I/O, no external dependencies
    integration: Component interaction tests — uses fixtures and engine pipeline
    regression: Known-input / known-output stability tests
addopts = -v
